# TypeScript on Node

The pipeline is written in TypeScript on Node and published as an npm CLI. Python was the considered alternative, on the grounds that `google-api-python-client` is Google's best-supported client library and its date handling is strong; TypeScript was chosen for familiarity and for the typed `googleapis` package.
